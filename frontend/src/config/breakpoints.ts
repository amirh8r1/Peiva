/**
 * Breakpoints سراسری سامانه — تنها نقطه تعریف عرض‌ها.
 * زیر DESKTOP_MIN_WIDTH پترن موبایل، از آن به بالا پترن دسکتاپ.
 */
export const DESKTOP_MIN_WIDTH = 768;
export const DESKTOP_QUERY = `(min-width: ${DESKTOP_MIN_WIDTH}px)`;
